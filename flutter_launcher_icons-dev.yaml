flutter_launcher_icons:
  android: true
  ios: true
  
  # Usa la versione in bianco e nero per iOS
  image_path: "assets/icon/app_icon_dev.png" 
  remove_alpha_ios: true
  
  # Adaptive Icons Android: sfondo grigio per capire subito che è l'app DEV
  adaptive_icon_background: "#9E9E9E" 
  adaptive_icon_foreground: "assets/icon/app_icon_foreground_dev.png"